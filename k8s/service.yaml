apiVersion: v1
kind: Service
metadata:
  name: fuego-cloud
  labels:
    app.kubernetes.io/name: fuego-cloud
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fuego-cloud
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
