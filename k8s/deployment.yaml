apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexo-cloud
  labels:
    app.kubernetes.io/name: nexo-cloud
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: nexo-cloud
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nexo-cloud
    spec:
      containers:
        - name: nexo-cloud
          image: ghcr.io/abdul-hamid-achik/nexo-cloud:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: nexo-cloud-env
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-secret
